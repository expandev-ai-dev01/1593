import { Router } from 'express';

const router = Router();

// Internal authenticated routes will be added here as features are implemented
// Example: Contact management, user profile, etc.

export default router;
